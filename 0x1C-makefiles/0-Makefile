# This is my first Makefile

# The name of the executable
TARGET = school

# The all rule builds the executable
all: $(TARGET)

# The $(TARGET) rule builds the executable
$(TARGET): school.c
	gcc -o $(TARGET) school.c

# Clean the build directory
clean:
	rm -f $(TARGET)
